{
  "name": "@magoco/core",
  "version": "0.0.1",
  "description": "MAGoCo core runtime — event bus, capability registry, session log, plugin loader",
  "type": "module",
  "main": "./src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "node --test --import tsx ./test/**/*.test.ts"
  },
  "dependencies": {
    "@magoco/web": "workspace:*",
    "@magoco/agents": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "tsx": "^4.19.0",
    "@types/node": "^22.0.0"
  }
}
