{
  "name": "@magoco/web",
  "version": "0.0.1",
  "description": "MAGoCo HTTP + WebSocket server — magoco.web.* capabilities",
  "type": "module",
  "main": "./src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "node --test --import tsx ./test/*.test.ts"
  },
  "dependencies": {
    "@magoco/core": "workspace:*",
    "@magoco/agents": "workspace:*",
    "esbuild": "^0.28.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "tsx": "^4.19.0",
    "@types/node": "^22.7.0"
  }
}
