{
  "name": "@magoco/workflows",
  "version": "0.0.1",
  "private": true,
  "description": "Placeholder — lands in its own phase",
  "type": "module",
  "main": "./src/index.ts",
  "scripts": { "build": "tsc -p tsconfig.json", "typecheck": "tsc -p tsconfig.json --noEmit" },
  "devDependencies": {
    "typescript": "^5.6.0",
    "tsx": "^4.19.0",
    "@types/node": "^22.0.0"
  }
}
