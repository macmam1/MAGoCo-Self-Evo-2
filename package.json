{
  "name": "magoco",
  "version": "0.0.1",
  "private": true,
  "description": "MAGoCo-Self-Evo-2 — standalone multi-agent framework with self-evolution",
  "license": "MIT",
  "packageManager": "pnpm@12.5.1",
  "scripts": {
    "build": "pnpm -r build",
    "test": "bash scripts/test-core.sh",
    "typecheck": "pnpm -r typecheck",
    "lint": "pnpm -r lint",
    "docker": "docker build -t magoco .",
    "dockerup": "docker compose up -d",
    "dockerdown": "docker compose down"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "tsx": "^4.23.15"
  }
}