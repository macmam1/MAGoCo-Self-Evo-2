{
  "name": "magoco",
  "version": "0.0.1",
  "private": true,
  "description": "MAGoCo-Self-Evo-2 — standalone multi-agent framework with self-evolution",
  "license": "MIT",
  "packageManager": "pnpm@12.5.1",
  "scripts": {
    "build": "pnpm -r build",
    "test": "bash scripts/test-core.sh",
    "typecheck": "pnpm -r typecheck",
    "lint": "pnpm -r lint"
  },
  "engines": {
    "node": ">=20"
  }
}
